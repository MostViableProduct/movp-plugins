.claude/settings.local.json
.claude/settings.json
.claude/scheduled_tasks.lock
.movp/config.local.yaml
.env.movp
# .mcp.json files contain credentials — generated by `npx @movp/cli init`.
# Match both root-level (./.mcp.json written by the CLI in consumer projects)
# AND one-deep (claude-plugin/.mcp.json etc. from any legacy config).
.mcp.json
*/.mcp.json
.DS_Store

# Local git worktrees
.worktrees/

# npm pack artifacts
*.tgz
package/

# scripts/mcp-smoke node_modules (installed by npm ci in CI; lockfile is committed)
scripts/mcp-smoke/node_modules/

# scripts/parse-tests node_modules (installed by npm install in CI; lockfile is committed)
scripts/parse-tests/node_modules/
